/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Vercel supports all Next.js features including API routes
  // No need for static export configuration
}

module.exports = nextConfig

